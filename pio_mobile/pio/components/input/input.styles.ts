import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    input: {
        width: '100%',
        marginVertical: 10,
        padding: 10,
        borderWidth: 1,
        borderColor: '#ccc',
        borderRadius: 5,
    },
    viewForm: {
        width: '100%',
    },
    textError: {
        color: '#EA4335'
    },
    textLabel: {
        color: '#000000'
    }
});

export default styles