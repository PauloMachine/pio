import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 16
    },
    title: {
        width: '90%',
        height: 14,
        borderRadius: 7,
        marginBottom: 5
    },
});

export default styles